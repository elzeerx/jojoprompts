export const IMAGE_BUCKET = 'prompt-images';
export const VIDEO_BUCKET = 'prompt-images'; // Use same bucket for videos
export const AUDIO_BUCKET = 'prompt-images'; // Use same bucket for audio
export const FILE_BUCKET = 'prompt-images'; // Use same bucket for files
export const DEFAULT_IMAGE_BUCKET = 'default-prompt-images';
